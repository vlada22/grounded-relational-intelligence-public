[project]
name = "grounded-relational-intelligence-public"
version = "1.0.0"
description = "Public aggregate evidence and static explorer for Article 03"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
